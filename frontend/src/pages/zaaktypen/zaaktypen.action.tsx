export type ZaaktypenActionData = object;

/**
 * Zaaktypen action.
 * Action data can be obtained using `useActionData()` in ZaaktypenPage.
 */
export async function zaaktypenAction(): Promise<ZaaktypenActionData> {
  return {};
}
