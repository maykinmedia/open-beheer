export type CatalogiActionData = object;

/**
 * Catalogi action.
 * Action data can be obtained using `useActionData()` in CatalogiPage.
 */
export async function catalogiAction(): Promise<CatalogiActionData> {
  return {};
}
