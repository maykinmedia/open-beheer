export type CatalogiLoaderData = object;

/**
 * Catalogi loader.
 * Loader data can be obtained using `useLoaderData()` in CatalogiPage.
 */
export async function catalogiLoader(): Promise<CatalogiLoaderData> {
  return {};
}
