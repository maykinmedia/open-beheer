export type ZaaktypecreateActionData = object;

/**
 * Zaaktypecreate action.
 * Action data can be obtained using `useActionData()` in ZaaktypeCreatePage.
 */
export async function ZaaktypeCreateAction(): Promise<ZaaktypecreateActionData> {
  return {};
}
